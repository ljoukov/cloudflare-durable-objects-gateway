{
	"name": "cloudflare-durable-objects-gateway",
	"version": "0.1.0",
	"description": "Bearer-authenticated HTTP gateway for configured Cloudflare Durable Object namespaces.",
	"type": "module",
	"private": true,
	"license": "MIT",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.3",
		"@cloudflare/workers-types": "^4.20260509.1",
		"typescript": "^6.0.3",
		"vitest": "^4.1.5",
		"wrangler": "^4.90.0"
	}
}
